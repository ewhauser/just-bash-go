# Repository Scripts

`scripts/` contains repository automation and reporting utilities.

The Go packages under this directory are used for:

- benchmark comparison and website benchmark data generation
- GNU compatibility result aggregation and HTML report rendering

The shell scripts in this directory support release, publishing, and mirror
update workflows. None of these paths are treated as stable runtime APIs.
